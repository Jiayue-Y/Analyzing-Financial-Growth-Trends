The time complexity of this program could range from O(n) to O(n+n^2), n being the length of the input array.
Though if th einput array size is 0, the time complexity will still be O(1).

The space complexity of this program is O(n).